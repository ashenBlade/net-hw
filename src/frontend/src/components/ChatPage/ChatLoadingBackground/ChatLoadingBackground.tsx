import React from 'react';

const ChatLoadingBackground = () => {
    return (
        <div className={'h-100 w-100 d-flex'}>
            <h1 className={'h1'}>Идет подбор...</h1>
        </div>
    );
};

export default ChatLoadingBackground;
